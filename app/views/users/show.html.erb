提问的问题:

<ul>
<%= render( :partial => 'questions/question' , :collection => @user.asked_questions )%>
</ul>

回答:

<ul>
  <%= render( :partial => 'questions/question' , :collection => @user.answers.map{|an| an.question} )%>
</ul>
